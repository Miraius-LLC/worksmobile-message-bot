---
name: large-scale-code-review
description: >-
  大規模・リポジトリ全体のコードレビュー / 包括的なコード監査を、claude・codex・agy の 3 エージェントが
  「同一スコープ・全観点を独立にレビューする三角測量 (triangulation)」方式で実施する skill。3 者は対等で
  重み付けせず、互いの指摘を見ずに (blind) 独立判断する。3 者が独立に一致した指摘＝高信頼、相違＝丸めず
  「意見差分」として 1 本のレビュー文書 (docs/reviews/) にまとめる。レビュー判断は ~/Develop/AGENTS.md §10
  に従い必ず agent-room 経由で他 agent に投げ、内部 self-review で代替しない。藤井の ~/Develop/ 配下で
  「大規模コードレビュー」「全体レビュー」「コード監査」「この repo をまとめてレビュー」「包括的にレビューして」
  「複数エージェントでレビュー」などと言われたら、agent-room を明示されなくても必ずこの skill を使う。
  数行の差分や 1 ファイルの軽い確認には使わない (それは通常の agent-room 単発レビュー)。
---

# Large Scale Code Review (3 エージェント三角測量レビュー)

大規模なコードベースを、**claude / codex / agy の 3 エージェントが全員・全観点を独立にレビュー**し、
結果を 1 本の文書に統合する。狙いは「1 人の見落とし」を 3 者の独立判断で潰すこと。

## この skill の中核原則 (絶対に崩さない)

1. **3 者は対等・全員が全観点をやる。** 観点を専門で分担しない。File 用 / Security 用…と役割分割しない。
   claude も codex も agy も、下の 6 観点すべてを同一スコープに対して通しでレビューする。重み付けしない。
2. **独立・blind・カンニング禁止。** 各エージェントは**他者の指摘を一切見ずに**自分だけで判断する。
   - 並列にディスパッチし、ある agent の出力を別 agent のプロンプトに入れない。
   - 「他のエージェントはこう言っている」「先行レビューでは…」を依頼文に**絶対書かない**(anchoring で独立性が壊れる)。
   - まとめ役も、他者の結果を読み込む**前に**自分の独立レビューを固める (自分の判断を他者に引っ張られない)。
3. **同調しない。** 各 agent は忖度・気兼ねで相手に合わせない。技術的根拠だけで判断する (§10 3 原則 #2)。
   統合段階でも相違を均さない。3 者が**独立に**到達した一致だけを「高信頼」とし、割れたものは「意見差分」で残す。
4. **レビュー判断は必ず agent-room 経由 (確定)。** ~/Develop/AGENTS.md §10 の絶対ルール。自セッション内の
   内部 self-review で他者レビューを代替しない。実レビューは agent-room post で実 agent に投げて取得する。
5. **完全 read-only (絶対)。** レビュアーはコードを書き換えない・worktree を作らない・実装しない・commit しない。
   見るだけ。各自の指摘は **MD ファイルに保存して報告**し、**まとめ役 (主エージェント) がそれらを 1 本に統合**する。

## レビュー 7 観点 (全 agent が全部やる)

1. **ファイル単位のコードレビュー** — 可読性 / 責務分離 / 命名 / 重複 / 設計の違和感
2. **共通化・保守性** — 共通化すべき処理 / 抽象化すべき箇所 / 逆に共通化しすぎない方が良い箇所 / 将来変更に弱い構造
3. **パフォーマンス** — 無駄な処理 / N+1 や過剰なループ / DB・API 呼び出しの非効率 / キャッシュ・遅延評価・バッチ化の余地
4. **テスト不足** — 単体 / 結合が要る箇所 / 境界値・異常系・権限系の不足 / 回帰テスト化すべき重要処理
5. **バグ・エラー温床** (`diagnose` skill 観点) — 例外処理不足 / null・undefined 想定漏れ / 型の過信 / 状態不整合 /
   日付・権限・トランザクション周りの危険箇所 / 本番障害につながる箇所
6. **セキュリティ・権限** — 認証・認可の弱点 / 入力検証不足 / 情報漏洩 / SQL・GraphQL・API 周りの脆弱性 /
   ログに出してはいけない情報 / 権限昇格の可能性。
   **DB がある場合は必ず**: RLS / 行レベル権限 / テナント分離 (cross-tenant 漏洩) / ロール境界
   (DB・外部入力由来の値を union/enum に `as` で無検証キャストして権限すり抜け、L11) / policy 依存列の変更漏れ (L24/L30)。
7. **不要コード・不要テストの削除提案** — デッドコード / 未使用 export・関数・依存 / コメントアウト放置 /
   形式だけで実質何も検証していないテスト / 仕様更新に追従できず古くなった (stale) テスト・doc・コメント。
   **削除「提案」に留める** (この skill では実削除しない)。

## 役割

- **まとめ役 (オーケストレータ)** = この skill を起動した agent。スコープ確定・ディスパッチ・統合・文書化を担う。
  - **Claude は遅いので、まとめ役・統合を codex に委ねてよい**。スコープが大きく Claude がボトルネックなら、
    オーケストレーション自体を `agent-room post --to codex` でまとめ役ごと委譲する判断をしてよい (要所は藤井に一言)。
- **3 人の独立レビュアー** = claude / codex / agy。まとめ役は自分自身の独立レビューを 1 本担い、
  残り 2 agent に agent-room で同一依頼を投げる (自己トリガ禁止: claude は `--to claude` しない、codex は `--to codex`、
  agy は `--to agy` しない。自分の分は自セッションで実施する)。結果 3 者の独立レビューが揃う。

## 手順

### Phase 0 — スコープ確定とコンテキスト把握 (まとめ役)

1. `git log --oneline -20` で HEAD を把握 (git-log ルール)。
2. **既定スコープ＝リポジトリ全体**。引数でパス / commit range / PR を渡されたらそれに絞る。
   スコープが巨大で曖昧なら藤井に確認 (L2: ユーザー依存の判断は推測せず確認)。
3. 関連 doc を読む (L3): `CONTEXT.md` (用語集) / `AGENTS.md` / `CLAUDE.md` / `docs/adr/`。ドメイン語彙と決定を踏まえる。
4. `git ls-files` でソースを列挙 → ディレクトリ / モジュール単位の**レビュー単位 (chunk)** に分割。
   巨大 repo は chunk 化し、**各 chunk を 3 者全員がレビュー**する (chunk を agent 間で分担しない＝三角測量を保つ)。
5. **agent-room の健全性を確認**: `agent-room status --health` / `agent-room doctor`。
   codex / agy の poller が落ちていたら**黙って省略せず**藤井に報告して指示を仰ぐ (no silent caps)。

### Phase 1 — 3 者の独立レビュー (並列・blind)

1. **依頼文を 1 つ作る** (下のテンプレート)。**全 agent に同一文面**を渡す (公平・独立)。
   依頼文に他者の見解を含めない。`repo:<project>`・対象 range/パス・7 観点・指摘フォーマット・**read-only**・
   **指摘を MD に保存して報告**することを明記。
2. **並列にディスパッチ** (直列にしない＝後発が先発を見て同調するのを防ぐ):
   - `agent-room post --from <self> --to codex -w "<依頼文>"`
   - `agent-room post --from <self> --to agy   -w "<依頼文>"`
   - まとめ役自身も**同じスコープを自分で独立レビュー**する (他者の返信を読む前に固める)。
   - 各レビュアーは指摘を **MD ファイルに保存して報告**する (headless poller は `.agent-room/<project>/reviews/` に
     全文 md を残す = §11。まとめ役自身の独立レビューも MD に書き出す)。まとめ役は Phase 2 でこれら 3 本の MD を統合する。
3. **観測は速い経路で**: terminal セッションからは `post --watch` を background で待たず、
   `Monitor` で `agent-room watch --since <run> --until codex,agy` を回して done を速く拾う
   (~/Develop/AGENTS.md §10、commit 476d2ba)。長文全文は `agent-room reply <run_id>` か `reviews/` の md を Read。
4. **受け取り方** (`receiving-code-review` の規律): 返ってきた指摘を鵜呑みにも全否定もしない。
   技術的に妥当か file:line で裏取りする。ただし**この段階では自分の独立レビューを書き換えない** (Phase 2 で突き合わせる)。
5. **L1 (confabulation 回避)**: 各レビューに `file_path:line` が伴うか検証。具体パスが無く一般論だけの返答は
   信頼せず、対象を絞って再依頼するか、まとめで「根拠薄」と明示する。

### Phase 2 — 統合 (まとめ役、1 本に集約)

3 者の独立レビューが揃ったら、**1 本のレビュー文書**に統合する。

- **まず重複排除 (合意度カウントの前提)**: 各指摘を **`file_path:行 + 種別`** で正規化キーにし、3 者の指摘を突き合わせる。
  同じ箇所・同じ種別を指していれば「同一指摘を n 者が独立に挙げた」と数える (行が数行ずれても、同じ問題を指していれば同一とみなす)。
  これをやらないと「3 者が同じ穴を見つけた」のか「別々の 3 件」なのか区別できず、合意度が無意味になる。
- **一致 (consensus)**: 3 者が**独立に**同じ指摘 → 高信頼。`合意度: 3/3` のように何者が挙げたかを記す。
- **部分一致**: 2/3 → 中。1/3 → 要検討として残す (独自の鋭い指摘も握り潰さない)。
- **意見差分**: 評価が割れた箇所 → 丸めず「意見差分」として両論を併記する (どちらが優れているとは断じない)。
- **重大度順** (Critical → High → Medium → Low) に並べる。
- **推測と事実を分ける**。仕様が不明な箇所は「仕様確認事項」として残す (勝手に断定しない)。
- まとめ役は各 agent の意見に**優劣を付けない・独断で省略しない・勝手に丸めない**。
- **殊勲指摘トップ 5 を最後に agent-room で発表し、各 agent の感想を集める**: 他 agent が気づけず単独で挙がった
  指摘 (合意度 1/3) を「貴重さ = 重大度 (Critical 重視) × 独自性」でランク付けし、上位 5 件と発見 agent を発表する。
  発表は **`agent-room post --from <self> --to codex,agy -w "..."`** で**他 agent を起こして**トップ 5 を共有し、
  それぞれの**感想**をもらう (例: この発見をどう見るか / 自分が見落とした理由 / 異論)。まとめ役自身の感想も添える。
  (自己トリガ禁止: まとめ役が claude なら `--to codex,agy`、codex なら `--to claude,agy`、agy なら `--to claude,codex`。)
  文書 (`docs/reviews/`) にトップ 5 と各 agent の感想を残す。

## 成果物 (1 本のみ)

`docs/reviews/code-review-<YYYY-MM-DD>.md` (JST 日付。`Date.now()+9h` してから `toISOString().slice(0,10)`、L25)。
同日再実行で衝突するなら `-<HHMM>` を足す。**ファイルは 1 本**。観点別に分割しない。

文書の構成:

```md
# コードレビュー <YYYY-MM-DD> — <project>

## サマリー
- 対象スコープ / レビュアー (claude / codex / agy) / chunk 数
- 重大度別の件数、最優先で対応すべき上位 N 件

## レビュアー別コスト (コスパ評価用)
| レビュアー | 完走時間 | 使用トークン | 有効指摘数 | 合意寄与 (n/3 に乗った数) |
|---|---|---|---|---|
| claude | … | … | … | … |
| codex  | … | … | … | … |
| agy    | … | … | … | … |
<!-- 藤井がコストに見合わない agent の配分を将来減らす判断材料にする。
     時間・トークンは各 agent の自己申告 (MD 末尾) + agent-room の run メタ (`agent-room query`/`status`) から拾う。 -->

## 指摘一覧 (重大度順)
<下の指摘フォーマットを重大度順に並べる。各指摘に「合意度 n/3」と挙げた agent を付す>

## 意見差分 (3 者で評価が割れた箇所)
<両論併記。丸めない>

## 仕様確認事項
<仕様不明で判断保留にした点>

## 殊勲指摘トップ 5 (他 agent が気づけなかった貴重な指摘)
<他のレビュアーが見落とし、単独で挙がった指摘を「貴重さ」でランク付けし上位 5 件を発表する。
 貴重さ = 重大度 (Critical 重視) × 独自性 (合意度 1/3 = 他者が見落とした)。
 各位に「どの agent が単独で拾ったか」を明記する (高くても貴重な発見をする agent を見極めるコスパ材料)。>
1 位: <指摘 / 発見した agent / file:line / なぜ貴重か>
2 位: …
3 位: …
4 位: …
5 位: …

### 各 agent の感想 (トップ 5 への所感)
- claude: …
- codex: …
- agy: …
```

### 指摘フォーマット

```md
## 指摘タイトル
- 対象: <file_path:line>
- 種別: <可読性 / 保守性 / パフォーマンス / テスト / バグ / セキュリティ・権限(RLS) / 不要コード>
- 重大度: Critical / High / Medium / Low
- 合意度: n/3 (claude / codex / agy のうち独立に挙げた数と内訳)
- 内容:
- 根拠:
- 影響:
- 改善案:
- テスト観点:
```

## 注意事項 (ガードレール)

- **完全 read-only (絶対)**。コードの書き換え・worktree 作成・実装・commit を一切しない。レビューと診断だけに集中する (修正は別タスク)。
- **agent-room 規律**: 自己トリガ禁止 / 依頼本文は日本語 (コード片・識別子・パス・range は原文可) /
  `repo:<project>` 明示 / read-only 明記 / deploy・本番操作はしない。
- 思想論に広げすぎない。具体のコード (file:line) に紐づけて語る。
- 重大度の高いものから優先的に整理する。
- 機微情報 (マイナンバー / 健康情報 / 口座番号) を成果物に出さない (~/.agents/AGENTS.md §4.4)。
- 成果物 `docs/reviews/` への commit は worktree 規律に従い、組織 repo (501 / asunaro 等) では push 前に藤井確認。

## 依頼文テンプレート (全 agent に同一文面)

```text
@<agent> repo:<project>
目的: 大規模コードレビュー (三角測量の独立 1 票)。あなたは他レビュアーの意見を見ずに、単独で全観点をレビューしてください。
対象: <commit range / PR / パス。無指定なら repo 全体 (HEAD)>
観点: 以下 7 つを全て見てください。
  1 ファイル単位 (可読性/責務/命名/重複/設計)
  2 共通化・保守性 (共通化/抽象化/過共通化回避/将来変更耐性)
  3 パフォーマンス (無駄処理/N+1/DB・API 非効率/キャッシュ余地)
  4 テスト不足 (単体/結合/境界・異常・権限/回帰)
  5 バグ・エラー温床 (例外/null/型過信/状態不整合/日付・権限・Tx)
  6 セキュリティ・権限 (認証認可/入力検証/情報漏洩/インジェクション/権限昇格。DB あれば RLS・行レベル権限・テナント分離・ロール境界も必須)
  7 不要コード・不要テストの削除提案 (デッドコード/未使用 export/形式だけ・stale なテスト/古いコメント・doc)
出力: 各指摘に file_path:line・重大度 (Critical/High/Medium/Low)・根拠・改善案・テスト観点。推測と事実を分ける。
      指摘は MD にまとめて報告 (reviews/ に全文)。**MD 末尾に自分の完走時間・使用トークン概算を必ず記す** (コスパ評価用)。
制約: read-only。コードを書き換えない・worktree を作らない・実装しない・commit しない (絶対)。push 済みコード (HEAD) を読む。他者の指摘に同調せず独立判断。
```

## 関連 skill / ルール

- `diagnose` — 観点 5 (バグ・エラー温床) の深掘りに使える。
- `receiving-code-review` — 返ってきた指摘の受け取り方 (鵜呑み / 全否定しない)。
- `requesting-code-review` — レビュー依頼の組み立て。
- ~/Develop/AGENTS.md §10 — agent-room 協業の SoT (主担当表・自然文指示の解釈・規律)。
- `docs/agent-room-cli.md` — agent-room CLI のコマンド表。
