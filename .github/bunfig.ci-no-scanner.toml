# CI 専用の install 設定。root の bunfig.toml から [install.security] だけを落としたもの。
# CI では install 時 scanner を使わないため、`scripts/ci-install.sh` が常にこれを使う。
# 理由と撤去条件は scripts/ci-install.sh の冒頭コメントを読む。
# 手元の install では使わない (root bunfig が正)。

[install]
